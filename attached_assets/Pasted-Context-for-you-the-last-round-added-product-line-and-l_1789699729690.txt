Context for you: the last round added product-line and loan-size-band gate dimensions plus an internal-conditions check. The tool now returns "Support not established" for all three sub-questions on the baseline ROBS fact pattern, which previously produced answers. This is a regression. Page 47 and page 93, both in Section A Core Requirements for all 7(a) and 504 Loans, are being excluded. Fix the gate semantics before anything else.

Change every gate dimension to default-permit. A chunk is excluded only on an affirmative mismatch, meaning the chunk carries a tag on that dimension and the fact pattern carries a conflicting value on the same dimension. Missing, empty, or unknown tags never exclude. Absent metadata means applicable, not inapplicable. This applies to transaction type, entity structure, party role, program scope, product line, and loan size band alike.
Tag universally applicable sections as such. Anything under Section A, Core Requirements for all 7(a) and 504 Loans, plus Appendix 3 Definitions and the general guaranty provisions, must carry a product-line value that matches every 7(a) product, or no product tag at all. Verify that page 47 and page 93 are admitted for the baseline pattern. If they are not, the gate is still wrong and nothing else in this round matters.
Make the conditions check fail open, not closed. If a condition cannot be extracted, or the fact pattern is silent on it, the provision stays admitted and the unresolved condition is noted on the card. Only an affirmatively failed condition, computed in code against a stated fact, excludes a provision. The 20 percent minority-investor threshold against a stated 68 percent is an affirmative failure. An unparsed condition is not.
Add a fail-safe floor. If every retrieved chunk for a sub-question is excluded, do not print "support not established." Instead print the highest-ranked retrieved provisions with a caution stating that each was excluded and why, so the user sees the candidate text and the reasoning. Reserve "support not established" for the case where retrieval itself returned nothing.
Add per-chunk gate telemetry. For every retrieved chunk log the chunk id, its tags on each dimension, the fact pattern's extracted values, which dimension excluded it, and the conditions check result. Print this to the console or a debug panel. Without it, the next regression is undiagnosable from the UI.
Fix the bottom line. The assumption note about no product being specified has taken over the bottom-line slot. Assumptions belong in their own one-line block beneath the version notice. The bottom line always states the substantive answer.
Regression gate before shipping. Assert that the baseline ROBS fact pattern returns a non-empty applied conclusion for every sub-question, with at least one admitted supporting provision each, and that page 47 and page 93 appear among them. Make this assertion a blocking test. Any change that empties the answer fails the build.

Then re-run the six cases from the previous round and report, for each, which provisions were admitted and which were excluded with reasons. If the positive controls still come back empty, the tagging pass from the previous round overwrote or dropped tags and needs to be rebuilt rather than patched.