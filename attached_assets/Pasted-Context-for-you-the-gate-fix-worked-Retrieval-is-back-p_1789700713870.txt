Context for you: the gate fix worked. Retrieval is back, page 47 and page 93 are admitted, the collapsed rejected pile and "Provisions to read" block render correctly, and the guarantor table now carries a real citation. Five defects remain. Item 1 comes first because without it the next regression is undiagnosable from screenshots, and item 2 is the user-visible headline failure.

Ship the per-chunk telemetry. This was requested last round and is not in the build. For every retrieved chunk, log and expose in a debug panel: chunk id, breadcrumb, page, its tags on every gate dimension, the values extracted from the fact pattern on those dimensions, which dimension excluded it if any, the conditions-check result with each condition and its pass/fail/unresolved state, which sub-question it was retrieved for, and whether it reached the applied-conclusion step. Also log what the conclusion synthesizer received and what it returned. Four rounds running, each fix has moved the failure one stage downstream, and that is a symptom of stage coupling that cannot be diagnosed from output alone.
The conclusion synthesizer fails closed and must fail open. The bottom line reads "No applied conclusion was established from the retrieved SOP provisions" while the same page shows three admitted, correctly cited provisions and a guarantor table marking a full unconditional guaranty as REQUIRED. The answer is present in the page and the summary denies it. Fix as follows. If one or more provisions are admitted for a sub-question, the synthesizer must write an applied conclusion from those provisions and may not emit the null state. The null state is reserved for zero admitted provisions. Do not require unanimity or agreement across admitted provisions; where they point different directions, state the conclusion the strongest applicable provision supports and note the tension in one clause. If the synthesizer cannot produce a conclusion from admitted provisions, that is a bug to surface in telemetry, not a state to render.
Sub-question routing is crossed. The guarantor table renders under the equity injection heading, and the guaranty provisions render under the guaranty heading with no table. Bind every artifact, meaning applied conclusion, supporting provisions, guarantor table, and rejected pile, to the sub-question id it was generated for, and render each under its own heading. Add an assertion that no artifact renders under a sub-question other than the one it was computed for.
Product-specific chapters require an affirmative product match. The CAPLines provision at page 150 carries SUPPORTS THIS CONCLUSION on a standard 7(a) change of ownership. Default-permit is correct for universally applicable sections and wrong here. Any chunk whose breadcrumb falls under a product-specific chapter, meaning CAPLines, MARC, Export Working Capital, International Trade, 7(a) Small Loans, SBA Express, or the 504 sections, is excluded unless the fact pattern's product affirmatively matches that chapter. These chapters are product-tagged by definition, so a missing tag on such a chunk is a tagging bug, not an unknown. Section A core requirements and the appendices keep default-permit.
The guarantor table still emits only one row. It shows the plan at 68 percent and nothing else, even though the page 47 sponsor provision appears in the supporting list immediately below and the buyer holds 32 percent directly. Fix the row generator to emit one row per party per capacity. For the baseline fact pattern that means at minimum: buyer as direct owner at 32 percent, buyer as plan sponsor under page 47, buyer as trustee executing on behalf of the trust, and the plan as entity owner at 68 percent. Add a blocking assertion that every party named in the fact pattern with a stated ownership percentage produces at least one row, and that every admitted provision imposing a guaranty obligation is cited as a triggering provision in at least one row. This gap has survived three rounds and a missing row means a missing signature at closing.

Blocking tests before this ships. On the baseline ROBS fact pattern, assert: a non-empty bottom line; a non-empty applied conclusion for every sub-question that has at least one admitted provision; page 47 and page 93 admitted; the page 150 CAPLines provision excluded on product line; at least four guarantor rows covering the capacities listed above; and every artifact rendered under its own sub-question heading. Any change that empties a conclusion or drops a guarantor row fails the build.

Then re-run the six regression cases from two rounds ago and report, for each, the admitted provisions, the excluded provisions with reasons, the guarantor rows produced, and the applied conclusion text. Include the telemetry output so the failures are traceable to a stage.