{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run python app.py",
    "start": "uv run gunicorn --bind 0.0.0.0:${PORT:-5000} --workers 2 --threads 4 app:app"
  }
}